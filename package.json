{
  "name": "prospektor-site",
  "version": "1.0.0",
  "description": "Prospektor — prospektor.ai: the landing page, the scan field, and the checkout that provisions a Partner Studio.",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "test": "node --test test/*.test.js",
    "drive": "npm run build && node test/drive.js",
    "audit": "node test/audit-live.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
